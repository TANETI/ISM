# ISM 업데이트 예정 항목

이 문서는 아직 공개 프롬프트와 사이트에 반영하지 않는 내부 작업 계획을 보관한다. 계획 단계의 내용은 `프롬프트/main prompt/`, 공개 문서, 사이트 UI에 `추가 예정`, `준비 중` 등의 메타데이터로 표시하지 않는다.

## 선택 의상 에셋

- `O`: 외출복 에셋 업데이트 예정.
- `S`: 수영복 에셋 업데이트 예정.
- 기존 `O`, `S` 폴더와 파일은 작업 자료로 보존하되 현재 사이트와 메인 프롬프트에서는 사용하지 않는다.
- 현재 `S`에 저장된 기존 이벤트 자료는 용도와 코드 체계를 재검토하기 전까지 공개하지 않는다.
- 공개 전 캐릭터별 완성 범위, 파일 번호, 이미지 비율, 명칭을 확정하고 사이트 회귀 검사를 통과해야 한다.

완료 후에만 관련 규칙과 노출 방식을 메인 프롬프트 및 사이트에 추가한다.
